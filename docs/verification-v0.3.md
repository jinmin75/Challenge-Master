# 로컬 시제품 v0.3 검증 기록

검증일: 2026-09-23. 대상은 공개 가능한 합성 과업과 실행 중 생성한 합성 PDF다. 실제 교재, 학생 계정, 설치파일, 학습효과는 검증하지 않았다.

| 확인 항목 | 실행 결과 | 범위 |
|---|---|---|
| `npm test` | 74 pass, 0 fail | 일정·기록·PDF·CLI·HTTP 화면 계약의 합성 회귀 |
| `npm run check` | 18개 JavaScript 모듈 구문·공백 통과 | 전체 린트·타입 검사 아님 |
| 합성 PDF 원본 비교 | 테스트가 만든 2쪽 PDF와 한글 글자가 든 PDF를 실제 pypdf로 읽음; 텍스트/빈 페이지 구분, 원본 바이트·SHA-256 보존 | 실제 스캔 OCR·표·수식·그림 충실도 제외 |
| PDF 검토 경계 | 원본 해시·변환 버전·초안 해시 불일치와 검토본 변조 거부; 명시 검토한 페이지만 faithful.md에 기록 | 사람의 육안 대조 행위 자체는 자동 검증하지 않음 |
| 주간 예측 | 휴식일 0분, 확인한 당일 시간 포함 0.9 시간 상한, 확인 기록만 차감, 미래 분량 tentative, 미배정 범위 유지 | 임용 준비 효과나 실제 주간 이행률 아님 |
| 로컬 HTTP 화면 | 임시 `127.0.0.1` 포트에서 `/`와 `/api/status` 각각 HTTP 200; API 흐름·교차 출처 거부 회귀 통과 | 브라우저 시각·접근성 사용 관찰 미실행 |

L09–L12의 로컬 일부를 확인했다. K01–K25의 제품 전체 시나리오와 L13 설치파일 검증은 NOT RUN이다. 현재 PDF 추출에는 개발 PC의 Python 3.12/pypdf 6.17.0이 사용됐다. 결과의 `needs_review`는 원본 대조가 남아 있다는 뜻이며, 준비 완료나 학생 숙달로 바꾸지 않는다.
